# name: test/sql/rt_write.test
# group: [sql]

require spatial_raster

statement ok
COPY (
	SELECT * FROM './test/data/mosaic/SCL.tif-land-clip00.tiff'
)
TO
	'__TEST_DIR__/copytoraster.tiff'
WITH (
	FORMAT RASTER, DRIVER 'COG', CREATION_OPTIONS ('COMPRESS=LZW')
);
